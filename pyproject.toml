[project]
name = "lmstudio-console"
version = "0.1.0"
description = "Remote web management dashboard for LM Studio"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115", "uvicorn>=0.32", "httpx>=0.27", "pydantic-ai>=2.0", "ddgs>=0.1"]

[dependency-groups]
dev = ["playwright>=1.40"]
